#include <stdio.h>

int main() {
    int a = 5, b = 2;
    printf("a + b = %d\n", a + b);
    printf("a - b = %d\n", a - b);
    printf("a > b? %d\n", a > b); 
    printf("a > 0 && b > 0? %d\n", a > 0 && b > 0);
    return 0;
}